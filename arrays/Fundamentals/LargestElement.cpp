// sort(arr.begin(), arr.end());
// return arr[n-1];

// TC = O(NLOGN), SC = O(1)

// ----------------------------------------------------
// int maxele = arr[0];
// for(int i = 1; i < n; i++){
//     if(arr[i] > maxele){
//         maxele = arr[i];
//     }
// }
// return maxele;

// TC = O(N), SC = O(1);